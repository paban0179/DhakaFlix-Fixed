buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // This is the engine that runs the Android plugin
        classpath 'com.android.tools.build:gradle:8.7.0'
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
